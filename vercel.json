{
  "buildCommand": "npm install && cd apps/frontend && TURBOPACK=false npm run build",
  "outputDirectory": "apps/frontend/.next",
  "env": {
    "NEXT_PUBLIC_NETWORK": "testnet",
    "TURBOPACK": "false"
  }
}
