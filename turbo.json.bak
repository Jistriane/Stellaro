{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "build/**"],
      "env": ["TURBOPACK=false", "NEXT_PUBLIC_NETWORK=testnet"]
    },
    "dev": {
      "cache": false
    },
    "lint": {},
    "test": {}
  }
}
